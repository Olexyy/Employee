@model IEnumerable<TweetChannelFollower>

@{
    ViewBag.Title = "Followed Channel";
    TweetUser user = ViewBag.TweetUser as TweetUser;
}

<h2>Followed tweets</h2>

@if(Model.Count() == 0)
{
    <p>No items in list</p>
}
else
{
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().TweetChannel.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().TweetUser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().TweetChannel.Created)
            </th>
            <th> Actions </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TweetChannel.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TweetChannel.TweetUser.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TweetChannel.Created)
                </td>
                <td>
                    @Html.TweetChannelFollowWidget(item.TweetChannel, user)
                </td>
            </tr>
        }

    </table>
}
