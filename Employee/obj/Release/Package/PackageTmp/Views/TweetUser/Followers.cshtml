@model IEnumerable<TweetUserFollower>

@{
    ViewBag.Title = "Followers";
    TweetUser user = ViewBag.TweetUser as TweetUser;
}

<h2>Followers</h2>

@if(Model.Count() == 0)
{
    <p>No items in list</p>
}
else
{
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.First().FollowerTweetUser.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.First().Created)
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FollowerTweetUser.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FollowerTweetUser.Created)
                </td>
            </tr>
        }

    </table>
}
