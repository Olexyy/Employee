@model IEnumerable<string>

@if (Model.Count() > 0)
{
    <ul id="country-list">
        @foreach(string address in Model)
        {
            <li onclick="selectCountry('@address');"> @address </li>
        }
    </ul>
}