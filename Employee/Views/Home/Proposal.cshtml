@model IEnumerable<string>

@if (Model.Count() > 0)
{
    <ul id="country-list">
        @foreach(string value in Model)
        {
            <li onclick="selectValue('@value');"> @value </li>
        }
    </ul>
}